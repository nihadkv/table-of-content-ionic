import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'scroll-content-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
